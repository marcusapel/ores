# Runtime requirements for the ORES web app (Python 3.12)
# Pinned FastAPI/Pydantic/Starlette to avoid IncEx import errors
fastapi==0.136.0
starlette==1.0.0
pydantic==2.13.2
uvicorn[standard]==0.44.0

# HTTP client / templating
httpx>=0.24.0
httpx[http2]
jinja2>=3.1.0
aiofiles>=23.1.0              # static file serving
python-multipart>=0.0.6

# Markdown rendering (HowTo pages)
markdown>=3.5
pymdown-extensions>=10.0

# Utilities
authlib>=1.3.0                # Azure AD PKCE auth flows
cryptography>=41.0            # Fernet encryption for token-at-rest
itsdangerous>=2.0             # Starlette SessionMiddleware cookie signing

# GraphQL (Reservoir DDMS RESQML deep-search on PostgreSQL)
strawberry-graphql[fastapi]>=0.220.0
asyncpg>=0.29.0               # Direct PostgreSQL access (same DB as OpenETPServer)

# Map rendering (Grid2d depth surfaces)
numpy>=1.24
matplotlib>=3.7

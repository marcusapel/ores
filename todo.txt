═══════════════════════════════════════════════════════════════════════
ORES: TODOs & Known Limitations
Updated: 2026-05-20
═══════════════════════════════════════════════════════════════════════

───────────────────────────────────────────────────────────────────────
1. PITFALLS & LESSONS LEARNED
───────────────────────────────────────────────────────────────────────

1.1  RDDMS REST API is EML-URI-centric
     ──────────────────────────────────
     REST listing returns {"uri": "eml:///dataspace('ds')/type(uuid)"}
     - no top-level UUID/ContentType keys.
     Fix: _parse_eml_entry() regex parser.

1.2  GET /resources wraps single object in a list
     Fix: Unwrap [obj] → obj in _rest_get_resource().

1.3  Underscore in dataspace name → manifest/build 500
     Server-side bug. Use names without underscore.

1.4  EPC XML ↔ RDDMS JSON schema divergence
     ETP --import-epc is the safest path. REST PUT needs hand-crafted
     JSON with $type annotations.

1.5  PropertyKind: two flavours
     StandardPropertyKind: {"Kind": "porosity"}
     LocalPropertyKind: {"LocalPropertyKind": {"Title": "..."}}
     Fix: _extract_property_kind() handles both.

1.6  Local RDDMS stack - architecture
     open-etp-client image (840 MB) = NestJS REST API, NOT a CLI.
     Includes manifest builder at POST /manifests/build.
     Env vars: RDMS_ETP_PROTOCOL, RDMS_ETP_HOST, RDMS_ETP_PORT,
               RDMS_REST_ROOT_PATH, RDMS_REST_PORT,
               RDMS_DATA_PARTITION_MODE, RDMS_JWT_SECRET.
     Local stack (docker-compose.yaml):
       postgres :5433  |  etp-server :9002  |  etp-client :3000
     Auth: any non-empty Bearer token accepted (authN=none).
     Manifest builder returns HTTP 201 (not 200) on success.

1.7  Radix ingress routes to wrong app
     ──────────────────────────────────
     https://web-ores-dev.c3.radix.equinor.com shows "OsduUi - Home"
     in the browser tab instead of ORES. The Radix ingress is
     routing to the OSDU UI container instead of the ORES pod.
     Likely M26 gateway/routing change.


───────────────────────────────────────────────────────────────────────
2. TODO / IMPROVEMENTS (code)
───────────────────────────────────────────────────────────────────────

2.1  DatasetIDs patch in GUI ingest flow
     ────────────────────────────────────
     CLI done (manifest_ingest.py). Remaining: wire the same
     DatasetIDs patch into keys_router.py /api/manifest/ingest.

2.2  Incremental / delta sync
     ────────────────────────
     Track lastChanged timestamps from RDDMS list response and
     only re-ingest changed objects.

2.3  Property kind → OSDU canonical mapping
     ──────────────────────────────────────
     UI widget to map LocalPropertyKind → Standard kinds per-dataspace.

2.4  deep_search performance - server-side bulk query
     ─────────────────────────────────────────────────
     Client-side property cache done. Need server-side "typeSearch"
     or "bulkGet" endpoint (requested: see MANIFEST_BUILDER_DEFICIENCIES.md §15).


───────────────────────────────────────────────────────────────────────
3. ADMIN REQUESTS (ORES deployment)
───────────────────────────────────────────────────────────────────────

3.1  Delete stale dataspaces
     ───────────────────────
     Delete: maap/drogon_osdu, maap/drogon2
     Keep:   maap/drogon (1302 obj), maap/drogon_dg (656 obj)

3.2  Radix ingress routes to OSDU UI instead of ORES
     web-ores-dev.c3.radix shows OsduUi tab title. Likely the
     ingress backend was inadvertently changed during M26 deploy.


───────────────────────────────────────────────────────────────────────
4. ROUNDTRIP FLOW
───────────────────────────────────────────────────────────────────────

  ┌─────────┐   ETP import    ┌──────────┐  manifest/build  ┌──────────┐
  │  .epc   │ ──────────────► │  RDDMS   │ ───────────────► │  OSDU    │
  │  file   │                 │ dataspace│                   │ catalog  │
  └─────────┘                 └──────────┘                   └──────────┘
       ▲                           │                              │
       │    ETP --export-epc       │     search / PG query        │
       └───────────────────────────┘     ◄────────────────────────┘

  Local stack (demo/drogonresqml/docker-compose.yaml):
  ┌─────────┐   ETP import    ┌──────────┐  manifest/build  ┌──────────┐
  │  .epc   │ ──────────────► │  local   │ ───────────────► │ manifest │
  │  file   │     ingest.sh   │  RDDMS   │  manifest_ingest │  .json   │
  └─────────┘                 │ :9002    │  (port 3000)     └────┬─────┘
                              └──────────┘                      │
                                                    push to     │
                                                    remote  ────┘
                                                    OSDU catalog


───────────────────────────────────────────────────────────────────────
5. ORES ROUTE LIMITATIONS
───────────────────────────────────────────────────────────────────────

  /graphql - deep_search
     Array threshold filtering needs PG access for cell values.
     (REST path implemented but RDDMS doesn't expose all arrays.)

  /graphql - federated_search
     Auto-discovers up to 50 dataspaces; searches 12 RESQML types.
     OSDU full-text search may produce false positives.

  /analyse - compare
     OSDU Search v2 can't filter nested array paths. Uses full-text
     workaround - may miss BDs.

  /keys - table view (STL resolution)
     Heuristic STL matching may pick wrong lookup table.

  /keys - structuremaps
     Time-domain Grid2d surfaces skipped (count shown in response
     as time_count / skipped_count).

  /strat - SMDA import/push
     Requires `az` CLI or SMDA_API_KEY. Non-functional without.

  /api/manifest/last
     In-memory only: max 200, 4h TTL, lost on pod restart.

  /search - wildcard kind queries
     ADME rejects kind:*. Auto-resolves via probe; results cached
     5 min to avoid repeated round-trips.
